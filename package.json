{
  "name": "saybutt",
  "version": "3.0.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "build": "node build.js",
    "now-build": "npm run build",
    "watch": "npx nodemon -e js,sh build.js"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
